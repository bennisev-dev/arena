# Pooled connection (app runtime). For Supabase use Session pooler URI + timeouts.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/arena"
# Direct connection (Prisma Migrate only). For Supabase: use Direct URI from dashboard + connect_timeout.
DIRECT_URL="postgresql://postgres:postgres@localhost:5432/arena"
JWT_SECRET="replace-with-a-long-random-secret"
JWT_EXPIRES_IN="7d"
COOKIE_DOMAIN=""

ELEAD_WEBHOOK_SECRET="replace-elead-secret"
FORTELLIS_WEBHOOK_SECRET="replace-fortellis-secret"
XTIME_WEBHOOK_SECRET="replace-xtime-secret"
DRIPJOBS_WEBHOOK_SECRET="replace-dripjobs-secret"
